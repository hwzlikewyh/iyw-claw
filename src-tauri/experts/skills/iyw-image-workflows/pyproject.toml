[project]
name = "iyw-image-workflows"
version = "0.0.1"
description = "Direct IYW commerce image workflow CLI"
requires-python = ">=3.10"
dependencies = ["pillow>=11.0.0"]

[tool.uv]
package = false
