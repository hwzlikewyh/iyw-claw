# Operational State Scaling

iyw-claw user memory owns durable memory scaling and review. This skill scales
only `~/.iyw-claw/self-improving/reflections.md`.

- Keep one concise entry per reusable process lesson.
- Remove exact duplicates during heartbeat.
- When the file becomes difficult to scan, summarize superseded operational
  entries under an `Archived summaries` heading in the same file.
- Never create project/domain user profiles or a parallel memory hierarchy.
- Never compact by deleting user-memory documents.
