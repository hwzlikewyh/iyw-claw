name: Prepare embedded Xinghe runtime
description: The runtime is compiled into the application; validate local pinned metadata
inputs:
  target:
    required: true
    description: Rust target triple
runs:
  using: composite
  steps:
    - uses: ./.github/actions/prepare-worker
      with:
        target: ${{ inputs.target }}
