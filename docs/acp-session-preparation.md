# ACP 会话预准备

## 2026-09-25 对话等待与计时口径

界面分别记录本轮开始后的首次活动、首次思考和正文首字。思考占位或工具调用
只推进对应时间，不再提前填入正文首字；历史恢复缺失的时间保持“未记录”。
工具处理阶段显示已运行时长与距上次输出时间，使用已有活动快照和可见界面的
本地时钟，不增加模型请求或网络轮询。状态变化会提示辅助技术，秒数刷新不反复播报。
当前用量契约未区分正文、思考与工具 token，因此原“平均输出”仍按上报用量/
整轮时间展示，不伪称为纯正文生成速度。

环境准备日志新增 `[ACP][runtime-env] stage`，以 runtime_trace_id 关联一次准备，
分别记录 storage_settings、agent_settings、platform_policy、profile_layout、
central_skills、managed_skills、shared_skills、provider_projection、session_migration、
runtime_credentials、xinghe_preferences、mcp_projection、tool_environment 和 node_preflight。
步骤失败或取消保留阶段；已有 best-effort 失败标为 degraded。日志不包含凭证或配置正文。
正常业务准备顺序和权限校验不变。

`scripts/analyze-agent-startup.mjs` 同时输出 runtimeEnvironmentStages 和 turnLatency，
后者区分 turn_first_event、turn_first_thinking、turn_first_content。统计旧版本日志时，
不能将工具等待或准备完成后用户停留的时长当成初始化耗时。

本轮全量 TypeScript、定向 ESLint、Rust 桌面可执行构建及运行时信息入口通过；
组件浏览器预览覆盖思考/工具/正文、计时与390px布局。尚未完成真实桌面新建/恢复
各5次验证：Windows known-folder API 忽略 USERPROFILE 覆盖，项目用户级文件仍会
共享系统账户目录；现有机器未安装 Windows Sandbox。需要独立系统账户或由用户
另行允许共享目录验证后才能继续，不能据此声明启动长尾已经消除。

## 目标与边界

星河与远山使用现有 ConnectionManager 提前完成原生新建/恢复、必需 MCP 目录和模型/模式确认。
用户打开时优先接管已经就绪的同一连接，命中路径目标是完整就绪 P95 不超过 200ms。
冷启动、未提前准备的历史及配置失效后的回退仍需正常启动；本次没有端到端实测证明全部场景达标。

其他 Agent 保留公共 ACP 连接复用、技能遍历和探测收尾优化；会话预准备当前仅对
`codex`、`claude_code` 开启，没有假设其他适配器具备相同恢复生命周期。

## 使用路径

- 历史侧栏项和非活动标签悬停或聚焦 150ms 后，提前请求恢复；离开前尚未触发的请求会取消。
- 当前自有会话真正就绪后（包括尚未发送的草稿），准备一个相同 Agent 的新会话。项目会话使用当前工作目录，
  无项目聊天分配独立 scratch 目录。用户新建聊天时该目录只允许领取一次。
- 准备不创建应用对话记录、不发送 prompt。既有原生初始化、MCP 握手与会话存储照常执行。
- 原生会话、MCP 和选择器全部就绪才允许接管。用户提前点击时等待正在进行的准备，不伪造 ready。
- 未命中、过期或配置变化时沿原入口回退；同一历史必须先等待被取消的准备连接释放。

## 生命周期

最多两个准备任务/未使用的就绪会话，正在清理的实例也占名额。有效期 120 秒，前端在
110 秒释放本地句柄。内存紧张或存储迁移时不创建新准备任务；配置失效、窗口关闭和应用退出
均取消准备。任务由既有 ConnectionTaskRegistry 跟踪，退出时统一收尾。

准备请求与运行时预热共用空闲预算准入锁，计入已保留的空闲连接、未接管准备与预热 Host。
准入繁忙或预算用尽时返回 null，让用户沿正常启动流程继续；不会为后台准备排队等待。

准备阶段通过 EventEmitter 的共享激活标记抑制外部事件；SessionState 仍正常更新并保存事件环。
普通发现、复用和连接列表不暴露未接管实例，实际发送入口拒绝尚未接管的连接。
接管在池锁内只执行一次，激活原事件目标并补发 SessionStarted，前端沿既有快照附着流程同步状态。

接管重新读取当前账号、有效环境、运行时版本、策略代际、MCP 配置、技能启用策略和相关设置。
同时比较项目/全局指令文件与 Skill 入口的元数据。目标、模式、模型和响应风格必须匹配。
这些检查失败时取消准备，不跳过认证、权限或模型确认来追求耗时。

仅删除本次分配、未领取且确实为空的 scratch 目录。已领取目录由既有草稿管理负责；原生初始化
在目录中留下文件时交给既有 orphan scratch GC，不递归删除用户或 Agent 已产生的文件。
这里没有新增原生 worker 共享、数据库 schema 或快照持久化格式。

## 接口与日志

桌面 Tauri 命令和服务器 POST 路由保持一致：

- `acp_prepare_session`：提交目标及当前偏好，返回准备句柄与工作目录；资源不足时返回 null。
- `acp_cancel_prepared_session`：取消尚未接管的准备，不影响已接管连接。
- `acp_reserve_prepared_workspace`：一次性领取新聊天的准备目录。
- `acp_connect`：自动尝试匹配准备结果，原有调用参数保持兼容。

`[ACP][prepare]` 记录 started、ready、失效、claim completed、retired。
`[ACP][startup] stage=prepared_session_lookup` 的 ready/miss 和 since_accepted_ms
可用于区分接管路径与冷启动。准备耗时不等同于点击后耗时；首次模型响应需继续单独统计。
日志不记录凭证、完整配置或对话正文。

## 2026-09-24 启动路径精简

- 前端只等待同一 Agent、同一会话或同一工作目录的准备请求；新聊天领取的目录
  会记录在准备项中，继续复用对应连接。其他目录的新建会话不再被无关准备请求阻塞，
  也不会在连接成功后取消无关的聊天准备。
- 准备指纹在数据库查询阶段筛选未删除的相关配置，保留原来的精确前缀过滤和
  配置变化检查，减少读取模型目录等无关数据。
- 启动前处理停用 Skill 时先筛选依赖关系，只为真正的依赖方读取 Agent 技能链接；
  没有依赖的 Skill 直接跳过。依赖仍在使用时保持原来的拒绝行为。

这些改动减少等待、数据读取和文件系统访问；尚无故障机器上的前后耗时对比，
不能据此保证 200ms 就绪，也不代表模型首条回复延迟已经缩短。

本轮通过静态调用链审查、Rust 语法/格式和修改范围 diff 检查。TypeScript 与 ESLint
运行数分钟未返回结果，Cargo 持续等待其他构建的目录锁，已结束本轮检查进程；
以下历史验证结果不代表本轮编译验证。未新增或运行测试。

## 2026-09-24 继续压缩就绪等待

- 运行时 Host 的四项能力使用一次平台目录读取和一次本地权限快照，数据库查询从最多
  11 次减为 2 次。每次仍调用原有权限求值器；HostExecution 关闭时不启用读、写或终端。
  无平台 ID、缺省偏好、数据库失败及远端策略分支保持原来的处理方式。
- 预准备指纹将原生配置、MCP 与指令文件读取放入阻塞线程，与权限和相关设置读取并行。
  哈希输入与配置变化比较保持一致，不引入跨请求的权限或凭证缓存。
- 未发送的自有草稿在真实就绪后也准备下一会话；远端查看者不触发额外准备，内存和空闲预算继续限制数量。
- 增加只读分析入口，按版本、Agent、恢复状态和来源分组，分别统计完整初始化、准备接管、各阶段和发送后的首次内容。

```powershell
node scripts/analyze-agent-startup.mjs '<安装根目录>\logs\iyw-claw.2026-09-24.log'
```

这里的就绪耗时从后端接受请求起算，不包含点击前后的前端调度。准备任务自身耗时使用
`source=preparation` 单列，不作为用户点击耗时。脚本输出每组样本数；少量样本的 P95
不能视为稳定的性能承诺，也不能将旧版本间的差异当作本次改动收益。

本轮日志分析已处理 2756 条合法记录，识别 5 条启动 trace，其中 1 条目录阶段失败。
0.1.242 的新建/恢复各只有 1 个成功样本，分别为 607ms/2712ms；没有准备接管样本。
前端定向 ESLint 通过（详情面板 2 条既有 Hook 警告）；全量 TypeScript 检查被本轮未修改的
`sidebar-filters.tsx` 翻译键类型错误阻塞，文件更新后的复查仍在第 228 行报告同类错误。
Rust 编译检查结束，唯一错误是本轮未修改的 `iyw_account.rs:465` 将 f64 积分赋给
`Option<i64>`；本次启动模块未报编译错误，完整构建未通过。尚未构建安装包或实测修改后性能。
上述为合并前的工作区验证结果，合并后的状态见下节。

## 2026-09-24 主分支集成复核

已读取远程 main 的最新提交 `ab82af6c`。当前工作分支通过 `9e104969` 合入该主分支，
并恢复了原有未提交改动。独立复核分支 `integrate/startup-main-20260924` 先以
`fc34cb24` 保存 14 个启动相关文件，再通过 `5b80e015` 合入同一主分支。

主分支已经包含当前旧分支 10 个提交的等价补丁。15 个冲突集中在旧版 MCP、内核补丁、
技能说明与 Cargo 锁文件，核对后采用主分支实现。独立合并结果相对 main 只包含本轮
启动修复、优化与诊断文件；运行时、依赖锁、内核及 MCP 与 main 一致。

复核覆盖进程内星河启动、聊天目录准备、准备任务的接管/取消、空闲预算与锁顺序、
配置指纹失效及 HostExecution 对子权限的约束，未发现新增逻辑冲突。
不依赖旧版独立 Worker，不因提速绕过真实就绪或配置校验。

验证输出保存在 `D:\projects\iyw\reports\startup-main-20260924`。本轮 Rust 格式检查、
相对 main 的 diff 检查和实际日志分析通过；定向 ESLint 通过，保留 2 条既有 Hook 警告。
Rust `cargo check --offline --locked --lib` 两次以 -1 异常结束，没有编译错误诊断；
完整 Rust 编译未完成。全量 TypeScript 检查通过（`tsc --noEmit --incremental false`）。
合并及提交均为本地操作，尚未推送、生成安装包或测量修改后的实机耗时。

## 历史验证

### 2026-09-24 失败重试与线程阻塞补修

- 新聊天目录准备失败后，再次聚焦输入框或发送消息会触发一次重试；进行中的创建
  继续由原来的单次执行标记保护。路径为空或只有空白时不发 ACP 连接，目录成功后
  沿原自动连接和消息队列继续，不生成替代历史会话。
- 内置/互联网/Office 技能对账与共享技能发布移入阻塞线程；原有文件写入锁随任务
  保持至结束，即使调用者取消，也不会提前释放后与下一次写入交错。
- 完整技能清单的目录遍历、内容哈希和分组也移入阻塞线程。保留所有 Agent 的观察、
  冲突、依赖、激活策略与插件判断，不以缩小校验范围换取速度。

新增本机日志样本：107 条启动 trace 中有 13 条 working_dir 错误；最长恢复为
96620ms，其中 runtime_env_reconcile 为 86421ms。另一条恢复的该阶段为 68112ms。
这些来自本机安装目录日志，不代表最初 Downloads 空日志对应用户的现场。已有安装
版本的一次准备接管为 75ms，不是本次修改后的性能测量。

本轮前端全量 TypeScript、定向 ESLint（2 条既有 Hook 警告）与生产静态导出通过。
独立工作树的 node_modules 为外部 junction，Turbopack 拒绝该目录链接，改用 Next.js
内置 Webpack 入口成功构建 35 个页面；应用配置未改变。
后台应用库 cargo check 已通过（67 条既有警告）；追加完整清单扫描后台化后的最终
复查也通过，结果见 cargo-final.log（EXIT_CODE=0）。按仓库规则未增加或运行测试套件。
启动相关源码已同步至主工作区；尚未打包发布或在故障用户机器运行。

按仓库 AGENTS.md，未新增或运行单元、集成、端到端测试。
桌面/服务器 cargo check 和 TypeScript 检查通过，另完成定向 ESLint 与静态生命周期审查。
新增前端模块与触发组件 lint 无错误；既有连接上下文保留 15 条原有格式问题，
详情面板保留一条原有 Hook 依赖警告。

未重新安装桌面应用，没有准备池命中的实机 P50/P95 数据，不能声明完整新建或恢复已经达到 200ms。
