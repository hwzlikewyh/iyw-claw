task: T06-desktop-bootstrap-upgrade-protection
repo: iyw-claw
owner: Task 13 (integration / rollout / delivery)
date: 2026-08-01

# Task 06 所有共享文件改动统一交给 Task 13 接线。以下为完整请求清单。
requests:
  - id: T06-IR-001
    target: src-tauri/src/lib.rs
    change: >
      invoke_handler 的 generate_handler! 列表中注册
      runtime_bootstrap_commands::bootstrap_init_status 与
      runtime_bootstrap_commands::bootstrap_initialize。
    reason: lib.rs 是共享总入口，Task 06 只提供导出函数。
    exported_by: src-tauri/src/commands/runtime_bootstrap.rs

  - id: T06-IR-002
    target: src-tauri/src/web/router.rs + src-tauri/src/web/handlers/runtime_bootstrap.rs
    change: >
      新增 web handler：POST /bootstrap_init_status（只读状态）与
      POST /bootstrap_initialize（触发初始化，body: taskId, channel?），
      内部调用 version_center installer 的 bootstrap_init_status /
      bootstrap_initialize，进度事件继续走 app://bootstrap-init。
    reason: router 与 web handler 是共享资源，Task 13 统一接线。

  - id: T06-IR-003
    target: .github/workflows/release-tauri.yml
    change: >
      “Verify bundled sidecars”步骤删除 uv/uvx 校验（prepare-sidecars.mjs 已不再
      产出 uv/uvx，仅 iyw-claw-mcp）；“Verify bundled runtime archives”步骤整体
      删除（resources/runtime 已不再 stage node/git/codex-acp）。
    reason: CI 校验与新的包内容不一致，不改则发布必失败。

  - id: T06-IR-004
    target: src-tauri/scripts/build-desktop.mjs
    change: >
      复核 sidecar preparation 步骤描述与 uv/uvx 相关残留；当前文件仅调用
      prepare-sidecars.mjs，功能上无需改动，若保留旧文案建议更新 label。
    reason: 共享构建脚本，Task 13 统一收口。

  - id: T06-IR-005
    target: src/i18n/messages/en.json + src/i18n/messages/zh-CN.json
    change: >
      Task 06 已将这两个文件恢复为 HEAD（此前被并行进程写入损坏）。
      并行任务若仍需新增 SkillMarketV2 等 key，请重新应用自己的改动；
      Task 06 前端组件文案已内联，不再依赖新 i18n key。
    reason: 避免多任务同时写同一 i18n 文件造成结构损坏。

  - id: T06-IR-006
    target: 远端 Windows CI
    change: >
      执行 06-desktop-bootstrap-upgrade-protection.md 的验证清单：
      包内容证明、干净机器在线初始化、断网/退出续传、同版本零下载、
      错摘要/签名/磁盘满不切 active、更新前后持久区摘要一致、LKG 回滚、
      并发窗口、活跃会话延迟激活。
    reason: 本机禁止编译/运行，上述行为验证只能在远端 CI 完成。
